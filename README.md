# COVID-19 Analysis

## Описание проекта

Проект предназначен для эпидемиологического анализа медицинских данных COVID-19 с использованием Apache Spark (PySpark). Основной целью является загрузка, предобработка, анализ и визуализация данных из COVID-19 chest X-ray dataset.

## Структура проекта

```
covid-epi-image-analysis/
├── analysis.ipynb                      # Основной Jupyter Notebook для анализа
├── data/                               # Директории для данных
│   ├── raw/                            # Сырые медицинские данные
│   │   └── covid-chestxray-dataset/    # COVID-19 dataset из GitHub
│   ├── processed/                      # Обработанные данные (Parquet формат)
│   └── results/                        # Результаты анализа
│       └── visualizations/             # Визуализации (PNG файлы)
├── src/                                # Исходный код Python
│   ├── preprocessing/                  # Модули предобработки данных
│   │   ├── data_loader.py              # Загрузка данных
│   │   ├── cleaner.py                  # Очистка данных
│   │   ├── missing_values.py           # Обработка пропущенных значений
│   │   ├── quality_assessment.py       # Оценка качества данных
│   │   └── age_categorization.py       # Категоризация возраста (UDF)
│   ├── analytics/                      # SQL аналитика
│   │   └── sql_interface.py            # Функции для SQL запросов
│   ├── visualization/                  # Визуализация
│   │   └── charts.py                   # Функции создания графиков
├── environment.yml                     # Конфигурация Conda окружения
├── README.md                           # Этот файл (документация для пользователей)
└── presentation.pdf                    # Презентация
```

## Установка окружения

### Требования к системе

- Python 3.10 или выше
- Конда (Conda) для управления зависимостями
- Минимум 16GB оперативной памяти (для обработки датасета >1GB)

### Пошаговая установка

1. **Клонирование репозитория**

```bash
git clone <repository-url>
cd covid-epi-image-analysis
```

2. **Создание и активация Conda окружения**

```bash
# Создание окружения из конфигурационного файла
conda env create -f environment.yml

# Активация окружения
conda activate rocm
```

**Примечание:** Окружение `rocm` уже предустановлено и содержит все необходимые зависимости:
- PySpark 4.1.0
- PyTorch 2.5.1+rocm (с поддержкой ROCm для GPU)
- Pandas 2.2.3
- NumPy 1.26.4
- Matplotlib 3.9.4
- Seaborn 0.13.2
- Plotly 6.5.0
- Jupyter Notebook и другие зависимости

Полный список пакетов см. в `environment.yml`.

3. **Проверка установки**

```bash
# Проверка версии Python
python --version

# Проверка установки PySpark
python -c "import pyspark; print(pyspark.__version__)"

# Проверка установки PyTorch и GPU
python -c "import torch; print(f'PyTorch: {torch.__version__}, CUDA: {torch.cuda.is_available()}')"
```

## Основные компоненты

### `analysis.ipynb` - Основной Jupyter Notebook

Главный рабочий блокнот для анализа данных, содержащий следующие основные секции:

1. Инициализация PySpark
2. Первоначальный анализ данных
3. Загрузка данных с явной схемой
4. Оценка качества данных
5. Обработка выбросов (Outliers)
6. Предобработка данных
7. Фильтрация данных
8. Сохранение в Parquet формат
9. SQL аналитика (5 обязательных запросов)
10. Визуализация данных (5 графиков с детальным анализом)
11. Итоговые выводы

### 📋 `environment.yml` - Конфигурация окружения

Определяет Conda окружение `rocm` с основными зависимостями:

**Основные пакеты:**
- `pyspark==4.1.0` - Apache Spark для Big Data обработки
- `torch==2.5.1+rocm6.2` - PyTorch с ROCm оптимизацией для GPU
- `pandas==2.2.3` - Анализ данных в памяти
- `numpy==1.26.4` - Численные вычисления
- `matplotlib==3.9.4` - Статическая визуализация
- `seaborn==0.13.2` - Продвинутая визуализация
- `plotly==6.5.0` - Интерактивная визуализация

## Как запускать анализ

### 1. Запуск Jupyter Notebook

```bash
# Активация окружения (если ещё не активировано)
conda activate rocm

# Запуск Jupyter Notebook
jupyter notebook analysis.ipynb

# Или Jupyter Lab для более продвинутого интерфейса
jupyter lab analysis.ipynb
```

### 2. Порядок выполнения ячеек

Рекомендуется выполнять ячейки последовательно сверху вниз:

1. **Ячейки 1-3**: Инициализация PySpark и проверка окружения
2. **Ячейки 4-8**: Первоначальный анализ данных, определение схемы, загрузка
3. **Ячейки 9-14**: Оценка качества и предобработка данных
4. **Ячейки 15-17**: Сохранение результатов и фильтрация
5. **Ячейки 18-23**: SQL аналитика (5 обязательных запросов)
6. **Ячейки 24-40**: Визуализация данных и детальный анализ каждого графика

## Устранение проблем

### Предупреждения PySpark
- Используется явная схема данных для устранения предупреждений "CSV header does not conform to schema"
- Конфигурация Spark: `spark.driver.bindAddress="127.0.0.1"` для решения сетевых предупреждений

### Рекомендации по производительности
- Для больших датасетов (>1GB) используйте Parquet вместо CSV
- Кешируйте часто используемые DataFrames: `df.cache()`
- Фильтруйте данные рано для уменьшения объёма обработки
